<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.listview">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListView">
        <activity android:name=".getImage"></activity>
        <activity android:name=".CustomList" />
        <activity
            android:name=".Grid"
            android:label="Gridview" />
        <activity
            android:name=".Horizontal_scrollview"
            android:label="Horizontal_Scrollview" />
        <activity
            android:name=".ScrollView"
            android:label="Vertial_SCrollview" />
        <activity
            android:name=".Web"
            android:label="WebView" />
        <activity
            android:name=".ListView_demo"
            android:label="ListView" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>